<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.iastate.coms309.project309">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:name="edu.iastate.coms309.project309.util.AppController"
        android:networkSecurityConfig="@xml/network_security_config"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".LoginActivity"
            android:label="@string/title_activity_login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".UserHomeActivity" />
        <activity
            android:name=".RegisterActivity"
            android:label="Register Account">
		</activity>
		<activity
            android:name=".DevHomeActivity"
            android:label="Developer Home" />
        <activity android:name=".QrScanner"  />
        <activity android:name=".UserProfileActivity" />
        <activity android:name=".Notification"
            android:label="Notifications">

        </activity>
    </application>

</manifest>
